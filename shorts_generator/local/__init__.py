"""On-device backends — download, transcribe, rank, and crop on your machine.

Requires the `local` dependency group (yt-dlp, mlx-whisper/faster-whisper,
opencv) installed via `uv sync`; hosted LLM ranking (LLM_PROVIDER=openai|gemini)
additionally needs the `cloud-llm` group.
"""
